main {    
    output(console_out, print)
    execute{
        signal:console_out = "Hello world through console";
        signal:print = "Hello world through signal";
    }
}

println {
    input (print)
    output (console_out, kill)    
    execute {
        /* This is an example comment*/
        signal:console_out = print;
        signal:kill = true;
    }
}