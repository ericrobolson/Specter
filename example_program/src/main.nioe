main {    
    output(print)
    execute{
        signal:print = "Hello world";
    }
}

println {
    input (print)
    output (console_out, kill)    
    execute {
        /* This is an example comment*/
        signal:console_out = print;
        signal:kill = true;
    }
}